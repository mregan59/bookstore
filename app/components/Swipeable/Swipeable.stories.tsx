import React from 'react'
import { storiesOf } from '@storybook/react-native'
import { Swiper } from './Swipeable'
import { View } from 'react-native'

storiesOf('Swiper', module).add('example', () => <Swiper />)
